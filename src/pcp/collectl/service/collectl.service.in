[Unit]
Description=pcp-collectl metric collection
Requires=pmcd.service
After=pmcd.service

[Service]
Type=forking
PIDFile=@PCP_RUN_DIR@/collectl.pid
ExecStart=@PCP_BINADM_DIR@/pcp-collectl -D
ExecReload=/bin/kill -USR1 $MAINPID

[Install]
WantedBy=multi-user.target
